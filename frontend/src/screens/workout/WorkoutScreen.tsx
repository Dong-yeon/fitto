/** 운동 — 설계서 2.4 (운동 기록 입력, 히스토리, 캘린더) */
import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function WorkoutScreen() {
  return (
    <ScreenPlaceholder
      emoji="💪"
      title="운동 기록"
      description="세트 기록 · 히스토리 · 캘린더 뷰 (phase 3)"
    />
  );
}
