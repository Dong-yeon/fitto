/** 로그인 — 설계서 2.1 / 3.1 (소셜·이메일 로그인) */
import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function LoginScreen() {
  return (
    <ScreenPlaceholder
      emoji="🔐"
      title="로그인"
      description="카카오/애플 소셜 로그인 및 이메일 로그인 (phase 2)"
    />
  );
}
