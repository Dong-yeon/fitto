/** 관계 선택 — 설계서 v2.0 2.1 ('커플 연결하기' / '트레이너입니다' / '나중에') */
import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function RelationSelectScreen() {
  return (
    <ScreenPlaceholder
      emoji="🔀"
      title="관계 선택"
      description="커플 연결하기 · 트레이너입니다 · 나중에 (phase 2)"
    />
  );
}
