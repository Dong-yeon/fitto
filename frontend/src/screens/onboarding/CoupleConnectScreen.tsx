/** 커플 연결 — 설계서 2.1 / 3.2 (초대코드 생성·입력) */
import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function CoupleConnectScreen() {
  return (
    <ScreenPlaceholder
      emoji="💞"
      title="커플 연결"
      description="6자리 초대코드 생성/입력으로 연결 (phase 2)"
    />
  );
}
