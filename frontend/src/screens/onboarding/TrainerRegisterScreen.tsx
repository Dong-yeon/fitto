/** 트레이너 등록 — 설계서 v2.0 2.1 / 3.2 REL-03 (이름·전문분야·소개·자격증) */
import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function TrainerRegisterScreen() {
  return (
    <ScreenPlaceholder
      emoji="🏋️"
      title="트레이너 등록"
      description="전문분야·소개·자격증 입력 후 즉시 등록 (phase 6)"
    />
  );
}
