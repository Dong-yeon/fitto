/** 회원가입 — 설계서 2.1 / 3.1 (이름·생년월일·성별·목표 운동) */
import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function RegisterScreen() {
  return (
    <ScreenPlaceholder
      emoji="📝"
      title="회원가입"
      description="이름·생년월일·성별·주간 목표 입력 (phase 2)"
    />
  );
}
