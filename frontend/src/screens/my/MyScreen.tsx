/** MY — 설계서 v2.0 2.2 (프로필·통계·설정 / 트레이너: 회원 관리·트레이너 설정) */
import React from 'react';
import { ScreenPlaceholder } from '../../components/ScreenPlaceholder';

export function MyScreen() {
  return (
    <ScreenPlaceholder
      emoji="👤"
      title="MY"
      description="프로필 · 통계 · 설정 (트레이너는 회원 관리 추가) (phase 5)"
    />
  );
}
